<view class="recommend-wrap">
  <view class="header">
    <image src="/static/images/home/recommend_bg.png" mode="aspectFill"></image>
    <view class="date">
      <text class="day">{{day}}</text>
      <text class="line">/</text>
      <text class="month">{{month}}</text>
    </view>
    <view class="play-more" bindtap="playAllSongSheet">
      <view class="play-left">
        <text class="iconfont icon-play"></text>
        <text class="play">播放全部</text>
      </view>
      <text class="iconfont icon-check"></text>
    </view>
  </view>
  <scroll-view wx:if="{{recommendList.length}}" scroll-y class="recommend-body"  style="height: calc(70vh - {{songInfo['id'] && songData['songUrl'] ? '93rpx' : 0}});">
    <music-item 
      wx:for="{{recommendList}}"
      wx:key="id" 
      data-index="{{index}}" 
      data-musicId="{{item.id}}" 
      bindmusicPlay="navigatePage"
      music-info="{{item}}"
      current-song-id="{{currentSongId}}"
    >
    </music-item>
    <view class="border-10"></view>
  </scroll-view>
  <view wx:else class="recommend-body recommend-no-data">暂无数据</view>
</view>
<music-control
  wx:if="{{songInfo['id'] && songData['songUrl']}}"
  is-play="{{isPlay}}"
  song-info="{{songInfo}}" 
  song-data="{{songData}}"
  >
</music-control>
